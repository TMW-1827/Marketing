# Зовнішній портал ТМ «Трускавецька»

Публічний портал для тих, з ким працює бренд: дистриб'юторів, торговельних
мереж, закладів HoReCa, кінцевих споживачів і партнерів спонсорських проєктів.

Зроблений на основі внутрішнього навчального порталу (`training-portal-src`):
та сама дизайн-система, та сама модель контенту, ті самі віджети каталогу й
логістики. Відрізняється аудиторією і, головне, тим, чого тут **немає**.

## Що всередині і чого немає

**Є:**

- **Вода** — головна, склад і властивості, джерело й Трускавець, три лінійки,
  каталог форматів. Це основний акцент порталу.
- **Співпраця** — дистриб'юторам, мережам, HoReCa, логістика, торговельне
  обладнання, якість і документи.
- **Спонсорство** — що ми підтримуємо, що даємо як спонсор, як подати заявку.
- **Довідка** — споживачам (з блоком питань і відповідей), матеріали бренду
  (логотипи в усіх форматах), довідник зі штрих-кодами й глосарієм, контакти.

**Свідомо немає:**

- цін, знижок, РРЦ, цінових зон, калькулятора націнки;
- обсягів, строків і умов постачання;
- внутрішніх матеріалів: тесту, сертифіката, скриптів продажу, роботи із
  запереченнями, обліку прогресу, входу за акаунтом.

Правило просте: у порталі лишається тільки те, що не змінюється від дати й від
контрагента. Усе комерційне узгоджується в договорі. Тому в моделі даних
(`src/types/catalog.ts`) немає навіть поля для ціни — зовнішній портал не може
її показати навіть помилково.

## Матеріали з Google Drive

Важкі файли — фото продукції й обладнання, бренд-гайд, специфікації,
фотоархіви — у бандл не потрапляють: 18 фото SKU важать понад 20 МБ. Портал
показує їх прямо з Drive через адресу прев'ю, тому тягне зменшені копії, а не
оригінали.

Для цього файли мають бути відкриті «усім, хто має посилання» — саме так вони
й налаштовані. Якщо доступ закриють, сторінка не зламається: `DriveImage`
пробує другу адресу, а потім прибирає зображення, і картка лишається
текстовою. Каталог обладнання в такому разі повертається до силуетів.

Ідентифікатори зібрані в трьох місцях: `src/content/assets.ts` (документи й
логотипи — сирі id, згруповані за варіантом знака), `src/data/photos.ts`
(фото SKU), `photoId` у `src/data/equipment.ts`.

Файли логотипів стоять просто під самим знаком у галереї, а не спільним
переліком унизу сторінки: партнер бачить варіант і тут же бере потрібний
формат. Те саме зі шрифтами. Кнопка веде на пряме завантаження, а не на
сторінку перегляду Drive.

Набір форматів у кожного знака — рівно той, що перелічений у бренд-гайді 2025
(AI, EPS, PDF, PNG, SVG для двох знаків із горами; AI, PDF, SVG, PNG для
текстових і SPORT). У теці на Drive лежить більше файлів — квадрати для
соцмереж, англомовна версія, EPS текстових версій, — але офіційним переліком
вони не позначені, тому на порталі їх немає.

Виняток — логотипи: три основні знаки (з горами, текстовий, SPORT) лежать
у `public/brand/` як SVG, разом важать 31 КБ. Вони мають бути видні завжди,
навіть офлайн. Поруч лежить обрізана копія знака з горами (`-crop.svg`) — ті
самі контури, змінено лише `viewBox`, бо оригінал зверстаний на аркуші A4 з
полями. Оригінальні файли з Drive не редагувалися.

## Один слоган

Слоган бренду в зовнішній комунікації один: **«Із самого серця Карпат»**.
Він заданий у `src/content/brand.ts` і підставляється в усі розділи звідти —
щоб змінити формулювання довелось в одному місці, а розійтись воно не могло.

## Розробка

```bash
npm install
npm run dev        # http://localhost:5174
npm run typecheck
npm run build      # → dist/
npm run build:demo # → dist-demo/index.html — усе одним самодостатнім файлом
npm run preview    # http://localhost:4173
npm run smoke      # перевірка сценаріїв у браузері; потрібен запущений preview
```

`build:demo` інлайнить у єдиний HTML увесь код, стилі та шрифти й не реєструє
service worker. Такий файл відкривається з диска і працює без мережі — зручно
показати портал за посиланням або надіслати вкладенням до погодження.

Для `smoke` потрібен браузер Playwright: `npx playwright install chromium`.

Перевірка не залежить від мережі: запити до Drive перехоплюються й
підмінюються зображенням-заглушкою. Так вона бачить, які саме адреси запитує
портал і чи малює він те, що прийшло, — і однаково проходить у пісочниці без
доступу до google.com.

## Публікація

Портал збирається в GitHub Actions (`.github/workflows/deploy-pages.yml`) і
публікується в підкаталозі `/partner-portal/`. Зібрана версія також лежить у
репозиторії в каталозі `partner-portal/` — щоб портал відкривався навіть без
запуску збірки.

Base у Vite відносний (`./`), маршрутизація — на хешах: та сама збірка працює
і в підкаталозі GitHub Pages, і з файлової системи в нативній обгортці.

## Структура

```
src/
  content/
    brand.ts          сталі бренду: слоган, назва, гаряча лінія, сайт
    assets.ts         посилання на файли й теки Google Drive
    portal.ts         збірка розділів у портал
    sections/         по файлу на розділ — чисті дані, без JSX
  types/
    content.ts        модель блоків контенту
    catalog.ts        модель товару (без комерційної частини)
  data/
    skus.ts           18 позицій: штрих-коди, палетизація, габарити, вага
    photos.ts         фото кожної позиції — id файлів на Drive
    equipment.ts      11 позицій торговельного обладнання з габаритами
    glossary.ts       глосарій для партнерів і споживачів
  features/
    catalog/          полиця, каталог, картка товару
    calculators/      калькулятор палети й таблиці
    equipment/        каталог обладнання із силуетами в одному масштабі
    reference/        глосарій
    widgets.ts        реєстр віджетів, на які посилається контент
  components/         рендерер блоків, навігація, пошук, іконки, DriveImage
  styles/             дизайн-токени бренду й стилі
public/
  brand/              три основні знаки бренду для показу на сторінці
```

Контент — це дані, а не розмітка: розділ описується масивом блоків, тому пошук
будується автоматично (з блоків витягується чистий текст), а самі розділи можна
перенести в CMS без переписування коду.

## Як додати розділ

1. Створити файл у `src/content/sections/` за зразком сусідніх.
2. Вказати `group` — одну з `water`, `cooperation`, `partnership`, `info`.
3. Додати імпорт і сам розділ у масив `sections` у `src/content/portal.ts`.

Меню, шторка розділів, перехід «далі» й пошук підхоплять розділ самі.
