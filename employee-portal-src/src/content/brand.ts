/**
 * Сталі бренду й порталу, на які посилається контент.
 *
 * Окремим модулем, а не полем портала: розділи читають ці значення під час
 * власної ініціалізації, а портал збирається вже з готових розділів —
 * імпорт із portal.ts дав би цикл.
 */

/**
 * Єдиний слоган бренду. Другого немає: у зовнішній комунікації бренд
 * говорить одну фразу, і саме її має вимовляти працівник — так само, як
 * вона надрукована на етикетці.
 */
export const SLOGAN = 'Із самого серця Карпат'

export const BRAND = 'Трускавецька'

/** Гаряча лінія бренду — та, що вказана на етикетці. */
export const HOTLINE = '0 800 30 18 27'

export const SITE = 'truskavetska.ua'

/** Адреса зовнішнього порталу — його можна давати клієнту. */
export const PARTNER_PORTAL = '../partner-portal/'

/** Навчальний портал із тестом і сертифікатом. */
export const TRAINING_PORTAL = '../training-portal/'

/** Портал обліку торговельного обладнання. */
export const EQUIPMENT_PORTAL = '../equipment-portal/'

/**
 * Редакція прайсу, за якою зібрані ціни в `data/skus.ts`.
 *
 * Показується поруч із кожною ціною на порталі: працівник має бачити, на
 * яку дату він дивиться, і не називати клієнту вчорашню цифру як сьогоднішню.
 */
export const PRICE_REVISION = 'липень 2026'
