name: Публікація сайту

# Збирає навчальний портал і публікує його разом зі звітами на GitHub Pages.
#
# Вимагає Settings → Pages → Source: GitHub Actions.
#
# Публікує ВЕСЬ сайт — звіти й портали з кореня репозиторію та
# свіжозібраний навчальний портал у /training-portal/. Усе лишається
# за тими самими адресами, що й за гілкової публікації.
#
# Не запускайте цей workflow, поки Source ще «Deploy from a branch»:
# GitHub тоді на кожен push збирає сайт сам, і дві публікації одного
# коміту блокують одна одну — обидві висять у черзі й падають за
# таймаутом на десятій хвилині.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Одночасно виконується лише одна публікація; зайві черги скасовуються.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: training-portal-src/package-lock.json

      - name: Встановити залежності порталу
        working-directory: training-portal-src
        run: npm ci

      - name: Зібрати портал
        working-directory: training-portal-src
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Зібрати сайт докупи
        run: |
          mkdir -p _site
          # Звіти та решта статики з кореня — без вихідних файлів порталу
          rsync -a --exclude '_site' --exclude 'training-portal-src' \
                   --exclude 'training-portal' --exclude '.git' \
                   --exclude '.github' ./ _site/
          # Свіжозібраний портал кладемо поверх того, що лежить у репозиторії
          mkdir -p _site/training-portal
          cp -r training-portal-src/dist/. _site/training-portal/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
